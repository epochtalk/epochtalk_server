defmodule Test.EpochtalkServerWeb.Controllers.PollJSON do
  use Test.Support.ConnCase, async: true
  alias EpochtalkServerWeb.Controllers.PollJSON

  # Specify that we want to use doctests:
  doctest PollJSON
end
