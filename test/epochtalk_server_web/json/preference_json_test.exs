defmodule EpochtalkServerWeb.PreferenceJSONTest do
  use EpochtalkServerWeb.ConnCase, async: true
  alias EpochtalkServerWeb.PreferenceJSON

  # Specify that we want to use doctests:
  doctest PreferenceJSON
end
