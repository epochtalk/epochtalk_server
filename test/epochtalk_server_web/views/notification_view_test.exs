defmodule EpochtalkServerWeb.NotificationViewTest do
  use EpochtalkServerWeb.ConnCase, async: true
  alias EpochtalkServerWeb.NotificationView

  # Specify that we want to use doctests:
  doctest NotificationView
end
