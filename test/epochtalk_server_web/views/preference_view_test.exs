defmodule EpochtalkServerWeb.PreferenceViewTest do
  use EpochtalkServerWeb.ConnCase, async: true
  alias EpochtalkServerWeb.PreferenceView

  # Specify that we want to use doctests:
  doctest PreferenceView
end
